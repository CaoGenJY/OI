#include <bits/stdc++.h>
using namespace std;

const int maxn = 4e4 + 4;
int n, m;
int a[maxn], mp[maxn], f[maxn];

int main() {
    cin >> n >> m;
    for (int i = 1; i <= n; i ++ ) {
        cin >> a[i];
        
    }
    return 0;
}